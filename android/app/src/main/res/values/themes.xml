<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      The visible UI is themed by Compose's MaterialTheme (see Theme.kt). This
      XML theme only needs to be a valid no-action-bar window theme, so we extend
      a platform Material theme and avoid pulling in appcompat/material-components.
      windowBackground matches the Compose surface to avoid a launch flash.
    -->
    <style name="Theme.AgentMaster" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>
